name: Build

on:
  push:
    branches: 
      - "**"

jobs:
  build:
    uses: ./.github/workflows/build.yml
    with:
      dotnet-version: '10.0.x'

  test:
    needs: build
    uses: ./.github/workflows/test.yml
    with:
      dotnet-version: '10.0.x'
      tests: '["LoggerTests"]'
